{
  "name": "rando-games",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "playtest": "node scripts/playtest.mjs",
    "reloadtest": "node scripts/reloadtest.mjs",
    "playtest:spaceship": "node scripts/playtest-spaceship.mjs",
    "playtest:bosses": "node scripts/playtest-bosses.mjs",
    "playtest:creative": "node scripts/playtest-creative.mjs",
    "balance": "node scripts/balance-report.mjs",
    "playtest:abilities": "node scripts/playtest-abilities.mjs",
    "subpath-check": "node scripts/subpath-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "@types/three": "^0.186.0",
    "playwright": "^1.56.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  }
}
